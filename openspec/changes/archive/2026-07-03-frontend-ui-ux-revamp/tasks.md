# Tasks: 前端 UI/UX 三階段優化重構待辦清單 (frontend-ui-ux-revamp)

## 1. 基礎準備與 Skeleton 開發
- [x] **任務 1.1**：設計在 `page.tsx` 中的加載骨架屏元件 `SearchSkeleton`，模擬搜尋卡片加載狀態（包含 `animate-pulse` 動效）。
- [x] **任務 1.2**：設計全文閱讀器加載骨架屏 `VerdictReaderSkeleton`，提供平滑流暢的預加載視覺體驗。

## 2. 智慧高亮與法條跳轉演算法
- [x] **任務 2.1**：實作 `highlightText` 輔助函式，支援 Token-based 正則替換，安全高亮被告、原告、法官與引用法條，並避免 HTML Tag 被巢狀破壞。
- [x] **任務 2.2**：在 `highlightText` 中加入法規名稱檢索網址生成，並渲染為指向中華民國全國法規資料庫的超連結。

## 3. 雙排版佈局切換與抽屜元件
- [x] **任務 3.1**：在 `page.tsx` 中新增 `layoutMode` 與 `selectedItem` 狀態。
- [x] **任務 3.2**：在搜尋結果面板頂部，新增現代化的切換按鈕，支援在「🔬 演算法三欄比對」與「📖 經典雙欄閱讀」之間切換。
- [x] **任務 3.3**：設計「經典模式」的佈局渲染：左側 35% 清單顯示 RRF 重排結果，右側 65% 常駐渲染智慧判決書閱讀器 `VerdictReader`。
- [x] **任務 3.4**：設計「右側滑出式抽屜 (Drawer)」元件：實作 Tailwind CSS 4 動畫、毛玻璃背景遮罩與點選遮罩關閉邏輯。
- [x] **任務 3.5**：重構現有的三欄卡片渲染，卡片未展開時亦支援「📖 閱讀全文」按鈕，點擊後觸發 Drawer 滑出。

## 4. 智慧閱讀器與段落導航
- [x] **任務 4.1**：實作 `VerdictReader` 組件/渲染區，將判決書區分為基本資訊、主文、事實與理由三個核心大區塊。
- [x] **任務 4.2**：在 `VerdictReader` 的側邊/頂部實作 Table of Contents (TOC) 目錄導航，整合 `scrollIntoView` 錨點平滑滾動。
- [x] **任務 4.3**：在 `VerdictReader` 底端整合相似案例推薦點選功能，點擊後觸發二次檢索。

## 5. 驗證與清理
- [x] **任務 5.1**：驗證三欄比對模式下 Drawer 開啟與關閉的滑動流暢度。
- [x] **任務 5.2**：驗證雙欄經典模式下，點選清單項目在右側快速渲染及 TOC 平滑跳轉的正常運作。
- [x] **任務 5.3**：執行 `npm run build` 確保 TypeScript 編譯與打包無任何靜態型別或語法錯誤。
